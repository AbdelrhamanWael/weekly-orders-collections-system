# 📦 تحويل المشروع إلى ملف .exe

دليل بناء برنامج **نظام ربط الطلبات والتحصيل** كملف تنفيذي واحد (.exe) حتى يعمل عند العميل **بدون تثبيت Python أو أي مكتبات**.

---

## 🔧 من يبني الـ .exe؟ (المطوّر)

### المتطلبات على جهازك
- **Python 3.10+** مثبت
- المشروع يعمل بشكل طبيعي عندك (`pip install -r requirements.txt` ثم تشغيل التطبيق)

### خطوات البناء

1. **تثبيت PyInstaller (مرة واحدة)**
   ```bash
   pip install pyinstaller
   ```

2. **تشغيل سكربت البناء**
   - من مجلد المشروع، شغّل:
   ```bash
   build_exe.bat
   ```
   أو يدوياً:
   ```bash
   pyinstaller --noconfirm --clean build_exe.spec
   ```

3. **النتيجة**
   - الملف الناتج: `dist\OrdersCollectionsSystem.exe`
   - انسخ هذا الملف للعميل (ملف واحد فقط).

### ملاحظات للبناء
- البناء قد يستغرق عدة دقائق.
- إذا ظهرت أخطاء عن مكتبة ناقصة، أضفها في `build_exe.spec` ضمن `hiddenimports=[]`.

---

## 👤 تشغيل البرنامج عند العميل

### ما يحتاجه العميل
- **لا يحتاج** تثبيت Python.
- **لا يحتاج** تثبيت أي مكتبات أو أيقونات إضافية.
- فقط **Windows 10 أو أحدث**.

### طريقة التشغيل
1. انسخ الملف `OrdersCollectionsSystem.exe` إلى أي مجلد (مثلاً سطح المكتب أو مجلد "البرنامج").
2. اضغط مرتين على `OrdersCollectionsSystem.exe`.
3. ستظهر نافذة سوداء (لا تغلقها) وسيفتح المتصفح تلقائياً على الصفحة الرئيسية للنظام.
4. للإيقاف: أغلق النافذة السوداء أو اضغط فيها `Ctrl+C`.

### المجلدات والملفات بجانب الـ .exe
عند أول تشغيل، البرنامج ينشئ بجانب الـ .exe تلقائياً:
- **samples** — لرفع ملفات الطلبات والتحصيل
- **reports** — لحفظ التقارير المُصدَّرة
- **finance_system.db** — قاعدة البيانات (تُنشأ تلقائياً)

العميل لا يحتاج إنشاء هذه المجلدات يدوياً.

---

## ❓ أسئلة شائعة

**س: هل يمكن وضع أيقونة مخصصة للـ .exe؟**  
ج: نعم. أضف في `build_exe.spec` السطر:
`icon='path/to/your_icon.ico'`
داخل `EXE(...)`.

**س: حجم الملف كبير؟**  
ج: نعم، غالباً بين 80–150 ميجا لأن Python والمكتبات (Flask, Pandas, openpyxl...) مضمّنة داخل الملف. هذا طبيعي.

**س: مضاد الفيروسات يمنع التشغيل؟**  
ج: أحياناً يظهر تحذير لأن الـ .exe غير موقع. يمكن للعميل إضافة استثناء للملف أو للمجلد، أو التوقيع على البرنامج لاحقاً إن رغبت.

---

**آخر تحديث:** فبراير 2026
